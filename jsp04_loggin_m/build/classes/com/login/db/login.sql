DROP SEQUENCE MYMEMBERSEQ;
DROP TABLE MYMEMBER;

CREATE SEQUENCE MYMEMBERSEQ;

--멤버번호, 아이디, 패스워드, 이름, 주소, 전화번호, 이메일, 가입여부(Y / N), 등급(ADMIN, USER, ...)
CREATE TABLE MYMEMBER(
	MYNO NUMBER NOT NULL,
	MYID VARCHAR2(500) NOT NULL,
	MYPW VARCHAR2(500) NOT NULL,
	MYNAME VARCHAR2(500) NOT NULL,
	MYADDR VARCHAR2(4000) NOT NULL,
	MYPHONE VARCHAR2(20) NOT NULL,
	MYEMAIL VARCHAR2(100) NOT NULL,
	MYENABLED VARCHAR2(2) NOT NULL,
	MYROLE VARCHAR2(100) NOT NULL,
	CONSTRAINT MYNO_PK PRIMARY KEY(MYNO),
	CONSTRAINT MYID_UQ UNIQUE(MYID),
	CONSTRAINT MYPHONE_UQ UNIQUE(MYPHONE),
	CONSTRAINT MYEMAIL_UQ UNIQUE(MYEMAIL),
	CONSTRAINT MYENABLED_CK CHECK(MYENABLED IN('Y','N'))
);

INSERT INTO MYMEMBER 
VALUES(MYMEMBERSEQ.NEXTVAL, 'admin', '1234', '관리자', '서울시 강남구', '010-1234-5678', 'ADMIN@naver.com', 'Y', 'ADMIN');

SELECT * FROM MYMEMBER ORDER BY MYNO DESC ;

SELECT * FROM MYMEMBER WHERE MYENABLED = 'Y';

UPDATE MYMEMBER SET MYENABLED = 'Y' WHERE MYNO = 1;
UPDATE MYMEMBER SET MYNAME = '이순신' WHERE MYNO = 1;

SELECT MYID FROM MYMEMBER WHERE MYID = '123';
